# =============================================================================
# AI Code Service — 环境变量配置模板
# =============================================================================
# 使用方式: cp .env.example .env  然后按需修改
# 优先级: 系统环境变量 > .env 文件 > config.yaml > 代码默认值
# =============================================================================

# -----------------------------------------------------------------------------
# 服务配置 (Server)
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=1235

# API Key 鉴权 (取消注释并设置后开启 Bearer Token 鉴权)
# API_KEY=sk-aicodeservice-secret

# -----------------------------------------------------------------------------
# 模型配置 (Model)
# -----------------------------------------------------------------------------
# 默认模型 (可选值见 config.yaml models 段)
DEFAULT_MODEL=qwen3.8-flash-next-oq4e-mtp-128k

# Mock 模式 (true = 不加载真实模型权重，用于测试/无 GPU 环境)
USE_MOCK=false

# -----------------------------------------------------------------------------
# 性能配置 (Performance)
# -----------------------------------------------------------------------------
# 是否启用 LRU + TTL 响应缓存
ENABLE_CACHE=true

# -----------------------------------------------------------------------------
# 测试配置 (Testing)
# -----------------------------------------------------------------------------
# 设为 1 以启用真实模型集成测试 (需要本地模型文件)
# RUN_REAL_MODEL_TESTS=1

# -----------------------------------------------------------------------------
# Web UI 网关配置
# -----------------------------------------------------------------------------
# Web 聊天界面端口
WEB_PORT=8080
# Python 后端地址
BACKEND_URL=http://localhost:1235
