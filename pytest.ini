[pytest]
pythonpath = .
testpaths = tests
addopts = -s
asyncio_mode = auto
markers =
    real_model: tests that load and run the local MLX model
filterwarnings =
    ignore::starlette.exceptions.StarletteDeprecationWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
